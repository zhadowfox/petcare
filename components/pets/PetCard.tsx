import Link from "next/link";import {Pencil,Trash2} from "lucide-react";import type {Pet} from "@/types/pet";export default function PetCard({pet,onDelete}:{pet:Pet;onDelete:(pet:Pet)=>void}){return <article className="card overflow-hidden"><div className="grid h-40 place-items-center bg-[#edf1eb]">{pet.photoUrl?<img src={pet.photoUrl} alt={pet.name} className="h-full w-full object-cover"/>:<span className="text-6xl">{pet.species==="Gato"?"🐱":pet.species==="Perro"?"🐶":"🐾"}</span>}</div><div className="p-5"><div className="flex items-start justify-between gap-2"><div><h3 className="text-xl font-black">{pet.name}</h3><p className="text-sm text-gray-500">{pet.species}{pet.breed?` · ${pet.breed}`:""}</p></div><span className="rounded-full bg-[#edf4ec] px-2 py-1 text-xs font-bold text-[#47634a]">{pet.gender}</span></div><div className="mt-5 flex gap-2"><Link href={`/mascotas/${pet.id}`} className="btn btn-primary flex-1">Ver ficha</Link><Link href={`/mascotas/${pet.id}/editar`} className="btn btn-secondary"><Pencil size={17}/></Link><button className="btn btn-danger" onClick={()=>onDelete(pet)}><Trash2 size={17}/></button></div></div></article>}
