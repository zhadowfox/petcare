"use client";import {useEffect,useState} from "react";import {useRouter} from "next/navigation";import Link from "next/link";import {onAuthStateChanged} from "firebase/auth";import {ref,uploadBytes,getDownloadURL} from "firebase/storage";import {auth,storage} from "@/lib/firebase/client";import {createPet} from "@/lib/services/pets";import PetForm from "@/components/pets/PetForm";import type {PetFormValues} from "@/lib/validations/pet";
export default function NewPet(){const router=useRouter();const[uid,setUid]=useState("");useEffect(()=>onAuthStateChanged(auth,u=>{if(!u)router.replace("/login");else setUid(u.uid)}),[router]);async function submit(data:PetFormValues,photo?:File){if(!uid)throw new Error("Sesión no disponible");let photoUrl="";if(photo){const r=ref(storage,`pets/${uid}/${crypto.randomUUID()}-${photo.name}`);await uploadBytes(r,photo);photoUrl=await getDownloadURL(r)}await createPet(uid,{...data,weight:data.weight??null,photoUrl});router.push("/dashboard")}return <main className="container-page max-w-3xl py-8"><Link href="/dashboard" className="text-sm font-bold text-[#5f7f61]">← Volver</Link><h1 className="mt-5 text-4xl font-black">Agregar mascota</h1><p className="mb-7 mt-2 text-gray-500">Crea la ficha de tu compañero.</p><PetForm onSubmit={submit}/></main>}
