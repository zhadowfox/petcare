import {addDoc,collection,deleteDoc,doc,getDocs,orderBy,query,serverTimestamp,updateDoc,where} from "firebase/firestore";import {db} from "@/lib/firebase/client";import type {Pet} from "@/types/pet";const ref=collection(db,"pets");
export async function getPets(ownerId:string){const q=query(ref,where("ownerId","==",ownerId),orderBy("createdAt","desc"));const s=await getDocs(q);return s.docs.map(d=>({id:d.id,...d.data()} as Pet))}
export async function createPet(ownerId:string,data:Omit<Pet,"id"|"ownerId">){return addDoc(ref,{...data,ownerId,createdAt:serverTimestamp(),updatedAt:serverTimestamp()})}export async function updatePet(id:string,data:Partial<Omit<Pet,"id"|"ownerId">>){return updateDoc(doc(db,"pets",id),{...data,updatedAt:serverTimestamp()})}export async function deletePet(id:string){return deleteDoc(doc(db,"pets",id))}
